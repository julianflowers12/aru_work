---
title: "GoogledriveR"
output: html_notebook
editor_options: 
  chunk_output_type: inline
---

<https://googledrive.tidyverse.org/index.html>

```{r}

install.packages("googledrive"); install.packages("gargle", clean = TRUE); install.packages("tidyverse", clean = TRUE)
library(googledrive)
library(gargle)
library(tidyverse)


```

```{r}
drive_auth(email = "julian.flowers12@gmail.com")

gargle::gargle_oauth_sitrep()

options(gargle_oauth_cache = ".secrets")
gargle::gargle_oauth_cache()
drive_auth(use_oob = TRUE)
#list.files(".secrets/")


```

```{r}
googledrive::drive_find()
googledrive::
googledrive::drive_get()
drive_ls(path = "ARU-protected-areas")

temp_dir = tempdir()
drive_download(path = "ARU-protected-areas", file = "WDPA_WDOECM_Aug2021_Public_all_csv.zip", type = "csv" ) 
drive_find(n_max = 10)
```
```{r}





```

